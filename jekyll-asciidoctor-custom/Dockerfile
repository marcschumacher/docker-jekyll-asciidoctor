# autogenerated by gen-user-img.sh on next

FROM machinekit/jekyll-asciidoctor-base

RUN groupadd --gid 130 jenkins
RUN adduser --disabled-password --uid 122 --gid 130 --gecos "jenkins user" jenkins
ENV HOME  /home/jenkins
USER jenkins

# in case we use it as a server
EXPOSE 35729 4000 80
